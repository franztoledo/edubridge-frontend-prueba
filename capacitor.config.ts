import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.edubridge.app', 
  appName: 'edubridge-frontend',
  webDir: 'dist/edubridge-frontend/browser',
};

export default config;